1. 变量类型

 1.1 logic 
  1）四值变量:  0， 1，x, Z.
  2）可在模块、接口、程序块、任务及函数声明中使用，但是不能用于多驱动。
  3）不支持多个持续性赋值或输出端口同时驱动同一变量。

1.2 automatic

   指定动态的存储方式，使得任务、函数或模块中的局部变量，每次在调用时获得独立的存储空间，而不是共享同一内存区域。

3. 过程语句和子程序
   
  
